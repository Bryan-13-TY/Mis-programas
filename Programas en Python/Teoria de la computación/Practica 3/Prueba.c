#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<conio.h>

int main(int argc, char const *argv[])
{
    int A,B,C,D,E,F,G,H,I,J,v,c;

    A = ( ( ( ( ( B + 10 ) * ( 9 ) ) + ( H % ( 3 * 10 * J ) ) ) + ( ( A + ( D * E ) ) / 32 ) ) ) ;
    A = B = C = D * 2;
    A = B * 3 = D * 2 ;
    A = B * C = D ;

    v = ( ( ( ( v * ( c ) ) + ( ( v ) / ( c ) % ( v ) ) ) ) * ( ( c + v ) * ( ( c / c ) ) ) ) ;

    printf("El resultado es %d",A);
    return 0 ;
}
